version: '3.8'
services:
  api:
    build:
      context: .
      target: test
    image: quizapp-api:test
    ports:
      - "31000:8080"
    environment:
      JWT_SECRET: aGVsbG93b3JsZAo=
      DB_URL: mongodb://mongo:27017
    depends_on:
      - mongo
  mongo:
    image: mongo
    logging:
      driver: "none"
